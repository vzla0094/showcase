import { DiscoveryScreen } from '../DiscoveryScreen'
import { renderWithProviders } from '../../../testing-utils'

describe('DiscoveryScreen', () => {
  it('should render', () => {
    renderWithProviders(<DiscoveryScreen />)
  })
})
