import { Button, Heading } from 'native-base'
import { ViewContainer } from '../atoms/ViewContainer'

export const CompanyScreen = () => {
  return (
    <ViewContainer>
      <Heading>Company screen</Heading>
      <Button>Create an Event</Button>
    </ViewContainer>
  )
}
